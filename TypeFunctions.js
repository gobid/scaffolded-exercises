/* TODO: turn to mjs once bundler is added */
/* Module creates utilities for different AST types so there are less switch statements all over the place */

export function print(nodeType) {
    
}

export function getComputedVal(nodeType) {

}

export function handlePartialUpdates(nodeType) {
    // may need something like this for handling cases like
    // when position[0] and 1 are updated separately?
}